name: Say hello

on:
  push: 
    branches:
      - "master"


jobs:
  hello_world: 
    runs-on: ubuntu-latest
    steps:
      - name: Echo del mensaje
        run: | 
          echo "hello react"
      - name: Echo fecha
        run: date
      - run: ls -l
          